version: 0.2

phases:
  install:
    commands:
      - npm install

  build:
    commands:
      - chmod +x scripts/build.sh
      - ./scripts/build.sh

artifacts:
  files:
    - '**/*'
  base-directory: build